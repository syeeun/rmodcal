#' @title Example Risk Model to Validate for 'rmodcal' Package
#' @description Pure cumulative risk model to validate with Cox proportional hazard. Example data \code{\link{data_valid}} can be used to validate this risk model (see example in \code{\link{model.calibration}}).
#' @format \code{survival::coxph} object.
"riskmod"